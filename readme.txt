Este archivo es de estudio/aprendizaje del framework ExpressJS,
perteneciente a NodeJS.

-La BD de prueba es provista por planetscale usando su plan gratuito.

El código se encuentra comentado, así que aquí no voy a poner
gran cosa.

-La carpeta views por lo general recibe ese nombre porque son las páginas
que el navegador va a poder ver. Con este módulo, Express gana una función
especial llamada render (se usa: res.render() )

-En la carpeta learning están los archivos que explican determinadas
cosas del curso de Fazt.